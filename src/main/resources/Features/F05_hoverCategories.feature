@smoke
Feature: F05_hover | user can use the hover feature

  Scenario: user can use the hover feature
    Given user select a random web element and hover over it
    And   user select a random sub web element and clicks on it
    #Then  user verify sub web element title is equal to the random sub web element selected
